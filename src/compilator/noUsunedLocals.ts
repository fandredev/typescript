function whateverFn(){
  let a // Erro, pois não estou usando essa variavel localmente
  return 'Olá'
}

/**
 * A opção 'noUnusedLocals' diz referente a valores e variaveis que não estão
 sendo utilizadas dentro ou fora de uma função
 */